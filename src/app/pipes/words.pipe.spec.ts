/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { WordsPipe } from './words.pipe';

describe('WordsPipe', () => {
  it('create an instance', () => {
    let pipe = new WordsPipe();
    expect(pipe).toBeTruthy();
  });
});
